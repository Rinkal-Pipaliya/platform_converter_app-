class ContactModel {
  String? image, name, contact, email, date, time;

  ContactModel(
      {this.image,
      this.name,
      this.contact,
      this.email,
      String? imagePath,
      this.date,
      this.time});
}
